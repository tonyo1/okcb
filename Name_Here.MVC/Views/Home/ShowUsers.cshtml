@using Name_Here;
@model List<Name_Here.User>

@{
    ViewData["Title"] = "ShowUsers";
}

<h1>ShowUsers - </h1>
<a asp-action="JsonView">Show JSON Document View</a>
@foreach (User item in Model)
{ 
    <div>
        <h4>User</h4>
        <hr />
        <dl class="row">
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => item.Email)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => item.Email)
            </dd>
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => item.Name)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => item.Name)
            </dd>
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => item.Role)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => item.Role)
            </dd>
        </dl>
    </div>
    <div>
        @Html.ActionLink("Edit - todo", "Edit", new { /* id = Model.PrimaryKey */ }) |
        <a asp-action="Index">Back to List</a>
    </div>
}